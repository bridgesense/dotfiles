" enables experimental transmutation module
let g:matchup_transmute_enabled = 1

" deferred highlighting to improve cursor movement performance 
let g:matchup_matchparen_deferred = 1

" treats <ul> / <li> tags like any other tag 
"let g:matchup_matchpref = { 'html': { 'nolists': 1 } }
